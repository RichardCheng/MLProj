import java.util.ArrayList;

public class EntryPool {
	
	/*
	public ArrayList<Entry> pool = new ArrayList<Entry>();
	
	public EntryPool (ArrayList<Entry> entrylist){
		pool = entrylist; 
	}
	
	public void genPredReport() {
		genPredReport(System.currentTimeMillis() + ""); 
	}
	
	public void genPredReport(String filename) {
		StringBuilder out = new StringBuilder(); 
		for (Entry e : pool){
			out.append(e.label + "\t" + e.predictedLabel + ": ");
			for (Double d : e.features){
				out.append(d + " "); 
			}
			out.append("\n");
		}
		Utilities.write(out.toString(), "log/" + filename + ".log"); 
	}
	*/
}